/* =====================================================================
   optional github token config ig
   ---------------------------------------------------------------------
   dont put a real token in any file you commit to github lol

   1. copy this to js/gh-config.js (its gitignored, so its safe)
   2. paste your token there

   leave it empty and the site just works unauthenticated (60 req/hr,
   which is fine for a small site).
   ===================================================================== */
var GH_TOKEN = '';